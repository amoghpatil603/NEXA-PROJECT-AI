version: '3.8'

services:
  nexa-platform:
    build: .
    ports:
      - "80:80"
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    volumes:
      - ./uploads:/app/uploads
      - ./data:/app/data
